# Core web framework
fastapi>=0.115.0
uvicorn[standard]>=0.32.0              # includes websockets + uvloop

# Computer Vision & ML
opencv-python-headless>=4.10.0         # headless version = perfect for servers/Docker
ultralytics>=8.3.0                     # latest YOLOv8/YOLOv11

# Database & ORM
sqlalchemy>=2.0.35
pydantic>=2.9.0                        # FastAPI uses Pydantic v2 by default

# Utilities
python-multipart>=0.0.12               # for file uploads
requests>=2.32.0
python-dotenv>=1.0.0
websockets>=13.0                       # usually pulled by uvicorn, but explicit is safer

numpy>=1.26.0,<2.1.0

tensorflow==2.18.0              # or upgrade to latest
tensorflow-intel==2.18.0        # if you specifically need the intel-optimized one